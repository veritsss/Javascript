// Math
let resultado;
resultado = Math.PI;
resultado = Math.round(2.7); // redondea ya sea hacia el piso o hacia arriba
resultado = Math.ceil(2.1); // SIEMPRE redondea hacia arriba
resultado = Math.floor(2.1); // SIEMPRE redondea al piso
resultado = Math.sqrt(144); // Calcula la raiz cuadrada
resultado - Math.abs(-200); // Convierte un numero negativo en POSITIVO
resultado = Math.min(3, 5, 1, 8, 2, 10); // Devuelve el valor mas bajo de una lista de valores
resultado = Math.max(3, 5, 1, 8, 2, 10); // Devuelve el valor mas alto de una lista de valores
resultado = Math.random(); // Devuelve el valor mas alto de una lista de valores
resultado = Math.floor(Math.random() * 30); // Multiplicamos un numero random entre (0-1) * 30 y lo redondeamos al piso

console.log(resultado);
